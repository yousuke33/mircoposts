
    <% @microposts = @micropost.find_by(id: like.micropost_id) %>
    <% @users = User.find_by(id: @microposts.user_id) %>
    <div class="container">
        <div class="row">
           <div class="col-md-3">
                <%= gravatar_for @users %> 
                <%= link_to @users.name, @users %>
           </div> 
           <div class="col-md-9">
               <%= @microposts.content %>
               <%= link_to 'unlike', like_path, method: 'delete' %>
           </div>
        </div>
    </div>
